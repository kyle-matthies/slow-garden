# Cost-model spike

Status: Passed as a planning-model probe on 2026-08-25

Question: Under conservative current model rates, do expected and stress alpha usage remain inside the proposed inference caps, and does a runaway schedule exceed them clearly enough for the no-run gate to activate?

Run:

```bash
node spikes/cost-model/cost-model.mjs
```

The rates are inputs, not embedded product policy. Refresh them from official provider pricing before enabling a model. Storage, attachment extraction, research tools, taxes, and optional backup services are intentionally excluded and must be added before those capabilities activate.

