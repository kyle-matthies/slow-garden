# Shared packages

Reserved for shared domain contracts, design tokens, provenance utilities, structured AI outputs, and evaluation tooling if the selected architecture needs them.

Do not create shared packages merely to satisfy a monorepo pattern; each package must have at least two real consumers or a clear independent verification boundary.
